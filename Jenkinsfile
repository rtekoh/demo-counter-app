pipeline {
    agent any 

    stages{
        
        stage{

            steps{
                git branch: 'main', url: 'https://github.com/rtekoh/demo-counter-app.git'
            }
        }
    }
}